en:
  atlas:
    hook:
      validation_hook:
        invalid_params: "Invalid parameters received to execute this action"
    service:
      telemetry:
        emit:
          invalid_parameters: "Invalid parameters received to emit telemetry event"
        adapter:
          firehose_adapter:
            service_unavailable: "AWS Firehose is unavailable, cannot emit event"
          kafka_adapter:
            service_unavailable: "Kafka is unavailable, cannot emit event"
      util:
        search_service:
          repository_failure: "Could not retrieve data"
